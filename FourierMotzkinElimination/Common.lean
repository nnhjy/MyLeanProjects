/-
  Common settings and packages
-/

import Mathlib.Tactic

set_option diagnostics true

-- `warningAsError` and `linter.style.emptyLine` are configured library-wide
-- in `lakefile.toml` ([lean_lib.leanOptions] for FourierMotzkinElimination).
